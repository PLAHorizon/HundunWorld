<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Horizon.Game.GengDi.Core.ViewModels"
             xmlns:models="using:Horizon.Game.GengDi.Models"
             xmlns:controls="using:Horizon.Game.GengDi.Core.Controls"
             xmlns:converters="using:Horizon.Game.GengDi.Core.Converters"
             x:Class="Horizon.Game.GengDi.Core.Views.WeatherDetailDrawerView"
             x:DataType="vm:HomeViewModel">

    <UserControl.Resources>
        <converters:HexToBrushConverter x:Key="HexToBrushConverter" />
    </UserControl.Resources>

    <Border Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
            BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}"
            BorderThickness="1 0 0 0"
            Width="420" MaxWidth="500"
            HorizontalAlignment="Right">
        <Grid RowDefinitions="Auto,*">
            <Border Grid.Row="0" BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}" BorderThickness="0 0 0 1" Padding="20 16">
                <Grid ColumnDefinitions="*,Auto">
                    <TextBlock Text="天气详情" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <Button Grid.Column="1" Content="✕" Classes="QuietAction" Padding="8 4" FontSize="14"
                            Command="{Binding CloseWeatherDetailCommand}" />
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Spacing="20" Margin="20 16">

                    <!-- 日出日落（全宽卡片） -->
                    <Border Background="#12FFFFFF" CornerRadius="16" Padding="16">
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Spacing="8">
                                <TextBlock Text="🌅 日出日落" FontSize="13" FontWeight="SemiBold" Foreground="#F1C96C" />
                                <StackPanel Orientation="Horizontal" Spacing="20">
                                    <StackPanel Spacing="2">
                                        <TextBlock Text="日出" FontSize="10" Foreground="#FFFFFF66" />
                                        <TextBlock Text="{Binding TodaySunrise}" FontSize="18" FontWeight="Bold" Foreground="#FFB74D" />
                                    </StackPanel>
                                    <StackPanel Spacing="2">
                                        <TextBlock Text="日落" FontSize="10" Foreground="#FFFFFF66" />
                                        <TextBlock Text="{Binding TodaySunset}" FontSize="18" FontWeight="Bold" Foreground="#FF8A65" />
                                    </StackPanel>
                                    <StackPanel Spacing="2">
                                        <TextBlock Text="日照" FontSize="10" Foreground="#FFFFFF66" />
                                        <TextBlock Text="{Binding DayLength}" FontSize="18" FontWeight="Bold" Foreground="#F1C96C" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                            <Canvas Grid.Column="1" Width="100" Height="50" VerticalAlignment="Center">
                                <Path Data="M 10,40 A 40,40 0 0,1 90,40" Stroke="#FFB74D44" StrokeThickness="1.5" StrokeDashArray="3,2" />
                                <Ellipse Canvas.Left="45" Canvas.Top="8" Width="10" Height="10">
                                    <Ellipse.Fill>
                                        <RadialGradientBrush>
                                            <GradientStop Color="#FFB74D" Offset="0.0" />
                                            <GradientStop Color="#FF8A65" Offset="1.0" />
                                        </RadialGradientBrush>
                                    </Ellipse.Fill>
                                </Ellipse>
                            </Canvas>
                        </Grid>
                    </Border>

                    <!-- 气象指标（4宫格，等高卡片） -->
                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto"
                          ColumnSpacing="12" RowSpacing="12">
                        <Border Background="#12FFFFFF" CornerRadius="16" Padding="16 14">
                            <StackPanel Spacing="6">
                                <TextBlock Text="🌬️ 风" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource BattleNetOutlineBrush}" />
                                <TextBlock Text="{Binding WeatherForecast.Current.WindDirection}"
                                           FontSize="24" FontWeight="Bold" Foreground="#FFFFFF" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{Binding WeatherForecast.Current.WindSpeed, StringFormat='{}{0:F0} km/h'}"
                                               FontSize="11" Foreground="{DynamicResource BattleNetOutlineBrush}" />
                                    <TextBlock Text="{Binding WeatherForecast.Current.WindLevel}"
                                               FontSize="11" Foreground="#5FD19B" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Background="#12FFFFFF" CornerRadius="16" Padding="16 14">
                            <StackPanel Spacing="6">
                                <TextBlock Text="💧 湿度" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource BattleNetOutlineBrush}" />
                                <TextBlock Text="{Binding WeatherForecast.Current.Humidity, StringFormat='{}{0}%'}"
                                           FontSize="24" FontWeight="Bold" Foreground="#FFFFFF" />
                                <ProgressBar Value="{Binding WeatherForecast.Current.Humidity}" Maximum="100"
                                             Height="4" Foreground="{DynamicResource BattleNetOutlineBrush}" Background="#1AFFFFFF" CornerRadius="2" />
                                <TextBlock Text="{Binding WeatherForecast.Current.DewPoint, StringFormat='{}露点 {0}°'}"
                                           FontSize="10" Foreground="#FFFFFF88" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="1" Background="#12FFFFFF" CornerRadius="16" Padding="16 14">
                            <StackPanel Spacing="6">
                                <TextBlock Text="📊 气压" FontSize="12" FontWeight="SemiBold" Foreground="#5FD19B" />
                                <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Bottom">
                                    <TextBlock Text="{Binding WeatherForecast.Current.Pressure, StringFormat='{}{0:F0}'}"
                                               FontSize="24" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Bottom" />
                                    <TextBlock Text="hPa" FontSize="11" Foreground="#FFFFFF88" VerticalAlignment="Bottom" Margin="0 0 0 3" />
                                </StackPanel>
                                <TextBlock Text="{Binding WeatherForecast.Current.PressureLevel}"
                                           FontSize="11" Foreground="#5FD19B" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="1" Background="#12FFFFFF" CornerRadius="16" Padding="16 14">
                            <StackPanel Spacing="6">
                                <TextBlock Text="☀️ 紫外线" FontSize="12" FontWeight="SemiBold" Foreground="#F1C96C" />
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock Text="{Binding WeatherForecast.Current.UvIndex}"
                                               FontSize="24" FontWeight="Bold" Foreground="#FFFFFF" />
                                    <TextBlock Text="{Binding WeatherForecast.Current.UvText}"
                                               FontSize="13" Foreground="#F1C96C" VerticalAlignment="Bottom" Margin="0 0 0 3" />
                                </StackPanel>
                                <ProgressBar Value="{Binding WeatherForecast.Current.UvIndex}" Maximum="11"
                                             Height="4" Foreground="#F1C96C" Background="#1AFFFFFF" CornerRadius="2" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- 能见度（全宽卡片） -->
                    <Border Background="#12FFFFFF" CornerRadius="16" Padding="16 14">
                        <Grid ColumnDefinitions="Auto,*,Auto" VerticalAlignment="Center">
                            <StackPanel Spacing="4">
                                <TextBlock Text="👁️ 能见度" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource BattleNetOutlineBrush}" />
                                <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Bottom">
                                    <TextBlock Text="{Binding WeatherForecast.Current.Visibility, StringFormat='{}{0:F0}'}"
                                               FontSize="24" FontWeight="Bold" Foreground="#FFFFFF" />
                                    <TextBlock Text="km" FontSize="11" Foreground="#FFFFFF88" VerticalAlignment="Bottom" Margin="0 0 0 3" />
                                </StackPanel>
                                <TextBlock Text="{Binding WeatherForecast.Current.VisibilityLevel}"
                                           FontSize="11" Foreground="{DynamicResource BattleNetOutlineBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" Spacing="4" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock Text="☁️" FontSize="10" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding WeatherForecast.Current.CloudCover, StringFormat='{}云量 {0:F0}%'}"
                                               FontSize="11" Foreground="#FFFFFF88" VerticalAlignment="Center" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border BorderBrush="#1AFFFFFF" BorderThickness="0 1 0 0" />

                    <!-- 未来天气 -->
                    <StackPanel Spacing="10">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="📅" FontSize="12" VerticalAlignment="Center" />
                            <TextBlock Text="未来天气" FontSize="13" FontWeight="SemiBold" Foreground="#FFFFFFBB" VerticalAlignment="Center" />
                        </StackPanel>
                        <ItemsControl ItemsSource="{Binding WeatherForecast.Daily}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="models:DailyWeather">
                                    <Border Padding="14 10" CornerRadius="10" Margin="0 0 0 6"
                                            Background="#0AFFFFFF">
                                        <Grid ColumnDefinitions="Auto,*,Auto" VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding WeekDay}" FontSize="13" FontWeight="Medium"
                                                           Foreground="#FFFFFFCC" Width="32" VerticalAlignment="Center" />
                                                <controls:WeatherIconControl WmoCode="{Binding WmoCode}"
                                                                             Width="28" Height="28" VerticalAlignment="Center" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center" Margin="12 0">
                                                <TextBlock Text="{Binding Condition}" FontSize="11" Foreground="#FFFFFFAA" />
                                                <Grid Width="100" Height="4" VerticalAlignment="Center" HorizontalAlignment="Left">
                                                    <Border CornerRadius="2" Background="#1AFFFFFF" />
                                                    <Border CornerRadius="2" Margin="{Binding TempBarMargin}"
                                                            Width="{Binding TempBarWidth}" Height="4"
                                                            HorizontalAlignment="Left">
                                                        <Border.Background>
                                                            <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                                                                <GradientStop Color="#7AA9FF" Offset="0.0" />
                                                                <GradientStop Color="#F1C96C" Offset="0.5" />
                                                                <GradientStop Color="#F26E7D" Offset="1.0" />
                                                            </LinearGradientBrush>
                                                        </Border.Background>
                                                    </Border>
                                                </Grid>
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <TextBlock Text="{Binding Precipitation, StringFormat='{}降水{0:F0}%'}" FontSize="9" Foreground="{DynamicResource BattleNetOutlineBrush}" />
                                                    <TextBlock Text="{Binding Humidity, StringFormat='{}湿度{0}%'}" FontSize="9" Foreground="#FFFFFF88" />
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Spacing="2">
                                                <TextBlock Text="{Binding TemperatureMin}" Foreground="{DynamicResource BattleNetOutlineBrush}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                                                <TextBlock Text="°" Foreground="{DynamicResource BattleNetOutlineBrush}" FontSize="10" VerticalAlignment="Center" />
                                                <TextBlock Text=" ~ " Foreground="#FFFFFF33" FontSize="9" VerticalAlignment="Center" />
                                                <TextBlock Text="{Binding TemperatureMax}" Foreground="#F26E7D" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                                                <TextBlock Text="°" Foreground="#F26E7D" FontSize="10" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>