<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Horizon.Game.GengDi.Core.ViewModels"
             xmlns:models="using:Horizon.Game.GengDi.Models"
             x:Class="Horizon.Game.GengDi.Core.Views.WeatherNewsDrawerView"
             x:DataType="vm:HomeViewModel">

    <Border Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
            BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}"
            BorderThickness="1 0 0 0"
            Width="420" MaxWidth="500"
            HorizontalAlignment="Right">
        <Grid RowDefinitions="Auto,*">
            <Border Grid.Row="0" BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}" BorderThickness="0 0 0 1" Padding="20 16">
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="天气资讯" FontSize="16" FontWeight="SemiBold" />
                    </StackPanel>
                    <Button Grid.Column="1" Content="✕" Classes="QuietAction" Padding="8 4" FontSize="14"
                            Command="{Binding CloseNewsDetailCommand}" />
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Spacing="8" Margin="20 16">

                    <ItemsControl ItemsSource="{Binding WeatherForecast.News}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="models:WeatherNews">
                                <Border Padding="10 8" CornerRadius="10" Margin="0 0 0 4" Background="#0AFFFFFF">
                                    <StackPanel Spacing="4">
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock Text="{Binding SourceIcon}" FontSize="10" />
                                            <TextBlock Text="{Binding Source}" FontSize="9" Foreground="#FFFFFF77" />
                                            <TextBlock Text="·" FontSize="9" Foreground="#FFFFFF33" />
                                            <TextBlock Text="{Binding TimeText}" FontSize="9" Foreground="#FFFFFF55" />
                                            <Border Background="#1A7AA9FF" CornerRadius="4" Padding="4 1" Margin="4 0 0 0">
                                                <TextBlock Text="{Binding Category}" FontSize="8" Foreground="{DynamicResource BattleNetOutlineBrush}" />
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="Medium"
                                                   Foreground="#FFFFFFEE" TextWrapping="Wrap" MaxLines="2" />
                                        <TextBlock Text="{Binding Summary}" FontSize="10" Foreground="#FFFFFF88"
                                                   TextWrapping="Wrap" MaxLines="2" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
