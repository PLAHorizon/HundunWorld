# 缓存策略

<cite>
**本文档中引用的文件**  
- [Cache.cs](file://Horizon.Core\Cache.cs)
- [ICache.cs](file://Horizon.Core.Abstract\ICache.cs)
- [RedisCache.cs](file://Horizon.Strategy.Storage.Redis\RedisCache.cs)
- [RedisConnection.cs](file://Horizon.Strategy.Storage.Redis\RedisConnection.cs)
- [CacheConst.cs](file://Horizon.Core\CacheConst.cs)
- [RedisOptions.cs](file://Horizon.Core\Options\RedisOptions.cs)
</cite>

## 目录
1. [引言](#引言)
2. [缓存抽象层设计](#缓存抽象层设计)
3. [Redis实现机制](#redis实现机制)
4. [缓存键命名规范](#缓存键命名规范)
5. [分布式锁应用](#分布式锁应用)
6. [缓存失效与保护策略](#缓存失效与保护策略)
7. [监控与性能测试](#监控与性能测试)
8. [环境配置差异](#环境配置差异)

## 引言
本系统采用Redis作为高性能数据缓存解决方案，通过抽象层设计实现了缓存功能与具体实现的解耦。该架构支持灵活的缓存策略配置、高效的并发控制以及可靠的缓存管理机制，为系统的高性能和高可用性提供了坚实基础。

## 缓存抽象层设计
系统通过定义`ICache`接口建立了统一的缓存访问契约，实现了业务代码与具体缓存实现的完全解耦。`Cache`静态类作为门面模式的实现，封装了所有缓存操作，提供了一致且简洁的API供上层调用。

```mermaid
classDiagram
class ICache {
<<interface>>
+bool IsClusterOpen
+int TimeOut
+Task<IDisposable> AcquireLockAsync(string key)
+Task<IDisposable> AcquireLockAsync(string key, TimeSpan timeOut)
+Task<bool> ExistsAsync(string key)
+Task<string> GetAsync(string key)
+Task<T> GetAsync<T>(string key)
+Task<bool> InsertAsync(string key, object data, int cacheTime)
+Task RemoveAsync(string key)
+Task<TimeSpan?> GetTimeToLiveAsync(string key)
+Task<bool> ExpireEntryInAsync(string key, TimeSpan expireIn)
+Task<double> GetCacheHitRate()
}
class Cache {
+static ICache Current
+static int TimeOut
+static bool IsClusterOpen
+static Task<IDisposable> AcquireLockAsync(string key)
+static Task<string> GetAsync(string key)
+static Task<T> GetAsync<T>(string key)
+static Task InsertAsync(string key, object data, int cacheTime)
+static Task RemoveAsync(string key)
+static Task<bool> ExistsAsync(string key)
+static Task<TimeSpan?> GetTimeToLiveAsync(string key)
+static Task<bool> ExpireEntryInAsync(string key, TimeSpan expireIn)
+static Task<double> GetCacheHitRate()
}
ICache <|.. RedisCache : 实现
Cache --> ICache : 依赖
```

**图源**
- [ICache.cs](file://Horizon.Core.Abstract\ICache.cs#L15-L319)
- [Cache.cs](file://Horizon.Core\Cache.cs#L10-L572)

**节源**
- [ICache.cs](file://Horizon.Core.Abstract\ICache.cs#L15-L319)
- [Cache.cs](file://Horizon.Core\Cache.cs#L10-L572)

## Redis实现机制
`RedisCache`类实现了`ICache`接口，基于StackExchange.Redis客户端库构建，提供了完整的Redis功能支持。通过`RedisConnection`连接池管理器确保了连接的高效复用和可靠性。

```mermaid
classDiagram
class RedisCache {
-RedisConnection _redisConnection
-int _defaultDb
+bool IsClusterOpen
+int TimeOut
+RedisCache(string connectionString, int db = -1)
+Task<IDisposable> AcquireLockAsync(string key)
+Task<bool> InsertAsync(string key, object data, int cacheTime)
+Task<string> GetAsync(string key)
+Task RemoveAsync(string key)
}
class RedisConnection {
-string _connectionString
-ConcurrentBag<ConnectionMultiplexer> _connectionPool
-SemaphoreSlim _poolSemaphore
+static RedisConnection Instance
+RedisConnection(string connectionString, int poolSize = 10)
+Task<IDatabase> GetDatabaseAsync(int db = -1)
+IEnumerable<EndPoint> GetEndPoints()
+IServer GetServer(EndPoint endpoint)
}
class RedisLock {
-IDatabase _database
-string _key
-string _token
+Task<bool> ExtendAsync(TimeSpan extensionTime)
+void Dispose()
}
RedisCache --> RedisConnection : 使用
RedisCache --> RedisLock : 创建
RedisLock --> IDatabase : 操作
```

**图源**
- [RedisCache.cs](file://Horizon.Strategy.Storage.Redis\RedisCache.cs#L9-L483)
- [RedisConnection.cs](file://Horizon.Strategy.Storage.Redis\RedisConnection.cs#L15-L337)

**节源**
- [RedisCache.cs](file://Horizon.Strategy.Storage.Redis\RedisCache.cs#L9-L483)
- [RedisConnection.cs](file://Horizon.Strategy.Storage.Redis\RedisConnection.cs#L15-L337)

## 缓存键命名规范
系统通过`CacheConst`类集中管理所有缓存键的命名规则，采用统一的前缀和分隔符，确保了缓存键的可读性和一致性。

```mermaid
flowchart TD
A[缓存键生成] --> B{是否包含参数}
B --> |否| C[直接使用常量]
B --> |是| D[组合前缀+名称+参数]
C --> E["iHuaxiaX-PASSPORTSPOOLS"]
D --> F["iHuaxiaX-VerificationUserData-type"]
G[前缀 iHuaxiaX] --> H[连接符 -]
H --> I[实体名称]
I --> J[参数值]
```

**图源**
- [CacheConst.cs](file://Horizon.Core\CacheConst.cs#L11-L149)

**节源**
- [CacheConst.cs](file://Horizon.Core\CacheConst.cs#L11-L149)

## 分布式锁应用
系统通过`AcquireLockAsync`方法实现分布式锁机制，有效防止了并发场景下的数据竞争问题，特别是在用户注册等关键业务流程中发挥了重要作用。

```mermaid
sequenceDiagram
participant ClientA as 客户端A
participant ClientB as 客户端B
participant Redis as Redis服务
ClientA->>Redis : AcquireLockAsync("passport_register_lock")
Redis-->>ClientA : 获取锁成功(token=guid1)
ClientB->>Redis : AcquireLockAsync("passport_register_lock")
Redis-->>ClientB : 获取锁失败(超时异常)
ClientA->>Redis : 执行注册逻辑
ClientA->>Redis : Dispose()释放锁
Redis->>Redis : LockRelease("lock : passport_register_lock", guid1)
```

**图源**
- [Cache.cs](file://Horizon.Core\Cache.cs#L100-L115)
- [RedisCache.cs](file://Horizon.Strategy.Storage.Redis\RedisCache.cs#L17-L35)

**节源**
- [Cache.cs](file://Horizon.Core\Cache.cs#L100-L115)
- [RedisCache.cs](file://Horizon.Strategy.Storage.Redis\RedisCache.cs#L17-L35)

## 缓存失效与保护策略
系统实现了完善的缓存生命周期管理机制，包括TTL设置、过期时间更新和穿透保护措施，确保了缓存数据的一致性和可靠性。

```mermaid
flowchart TD
A[缓存操作] --> B{操作类型}
B --> |插入| C[设置TTL]
B --> |查询| D{是否存在}
D --> |是| E[返回缓存数据]
D --> |否| F[查询数据库]
F --> G{有数据?}
G --> |是| H[写入缓存并返回]
G --> |否| I[写入空值缓存防止穿透]
C --> J[TTL=配置值]
H --> K[设置合理TTL]
I --> L[设置短TTL防止长期污染]
```

**图源**
- [RedisCache.cs](file://Horizon.Strategy.Storage.Redis\RedisCache.cs#L100-L150)
- [Cache.cs](file://Horizon.Core\Cache.cs#L200-L250)

**节源**
- [RedisCache.cs](file://Horizon.Strategy.Storage.Redis\RedisCache.cs#L100-L150)
- [Cache.cs](file://Horizon.Core\Cache.cs#L200-L250)

## 监控与性能测试
系统提供了缓存命中率监控接口，并可通过基准测试评估缓存性能表现，为性能优化提供数据支持。

```mermaid
erDiagram
CACHE_METRICS {
string metric_name PK
double value
datetime timestamp
string environment
}
CACHE_HIT_RATE {
string cache_key PK
int hit_count
int miss_count
double rate
datetime last_updated
}
PERFORMANCE_TEST {
string test_id PK
string scenario
int concurrency_level
double avg_response_time
double throughput
double hit_rate
datetime test_time
}
CACHE_METRICS ||--o{ CACHE_HIT_RATE : 包含
CACHE_METRICS ||--o{ PERFORMANCE_TEST : 记录
```

**图源**
- [ICache.cs](file://Horizon.Core.Abstract\ICache.cs#L300-L310)
- [RedisCache.cs](file://Horizon.Strategy.Storage.Redis\RedisCache.cs#L450-L460)

**节源**
- [ICache.cs](file://Horizon.Core.Abstract\ICache.cs#L300-L310)
- [RedisCache.cs](file://Horizon.Strategy.Storage.Redis\RedisCache.cs#L450-L460)

## 环境配置差异
不同环境下的Redis配置存在显著差异，主要体现在连接字符串、超时设置和资源分配等方面，以适应各环境的特性和需求。

```mermaid
graph TB
subgraph 开发环境
A[localhost:6379]
B[无密码]
C[单节点]
D[低超时]
end
subgraph 生产环境
E[集群地址]
F[带密码]
G[主从复制]
H[高超时]
I[连接池]
end
A --> |开发测试| J[开发者]
E --> |高可用| K[生产服务]
style 开发环境 fill:#f9f,stroke:#333
style 生产环境 fill:#bbf,stroke:#333
```

**图源**
- [RedisOptions.cs](file://Horizon.Core\Options\RedisOptions.cs#L8-L11)
- [RedisConnection.cs](file://Horizon.Strategy.Storage.Redis\RedisConnection.cs#L40-L55)

**节源**
- [RedisOptions.cs](file://Horizon.Core\Options\RedisOptions.cs#L8-L11)
- [RedisConnection.cs](file://Horizon.Strategy.Storage.Redis\RedisConnection.cs#L40-L55)