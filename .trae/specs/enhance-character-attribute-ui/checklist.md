# Checklist

## 3D 角色实时预览
- [x] `CharacterPreview3D` 控件已创建，使用 RenderTexture + 独立 Camera + AnimatedModel 实现 3D 渲染
- [ ] 打开角色属性面板时左侧预览区显示当前角色 3D 模型，居中等比缩放（代码已实现，待运行时验证）
- [x] 鼠标左键拖拽可旋转角色（绕 Y 轴），松开后保持角度
- [x] 屏幕尺寸变化时 RenderTexture 分辨率与相机位置同步调整
- [x] 3D 预览未污染主场景渲染（独立 Camera 离屏渲染）

## 六边形雷达图叠加
- [x] `HexRadarChartOverlay` 控件已创建，支持六边形网格背景
- [x] 外层五行数据多边形（金木水火土 + 中和）使用五行元素色绘制
- [x] 内层关键属性多边形（攻击/防御/气血/暴击/命中/闪避）使用金色半透明填充
- [x] 两组多边形叠加显示，外层描边发光、内层细描边，视觉层次清晰
- [x] 顶点悬停检测正常，鼠标靠近顶点时触发 Tooltip 显示
- [x] 属性变化时雷达图带动画过渡（复用插值动画机制）

## 属性 ToolBar 悬停提示
- [x] `InkAttributeTooltip` 控件已创建，三段式布局（头部图标 + 中部核心信息 + 下部附加信息）
- [x] 悬停基础属性（气血/体魄/内力/身法/根骨/悟性）显示对应 Tooltip
- [x] 悬停进阶属性（暴击/抗暴/命中/闪避）显示百分比与具体数值
- [x] 悬停五行属性显示元素图标 + 亲和度数值 + 相生相克说明
- [x] 悬停装备槽显示装备图标 + 名称 + 品质 + 等级 + 属性加成列表
- [x] Tooltip 自动定位避免超出屏幕边界
- [x] 下部附加信息支持可追加内容（如装备来源列表）

## 装备双击切换
- [x] 双击背包物品可装备到对应槽位
- [x] 双击已装备槽位可卸下装备到背包
- [x] 装备切换后角色属性立即追加或移除装备属性值（BaseStats + WuxingBonus）
- [x] 装备切换后雷达图、InkBar、3D 预览同步刷新
- [x] 槽位已有装备时，新装备替换原装备，原装备返回背包
- [x] 背包已满时卸下装备显示"背包已满"提示
- [x] 等级不足时装备物品显示"等级不足"提示
- [x] `InkEquipmentSlot` 与 `InkBackpackGrid` 双击事件正常触发

## 顶部信息扩展（昵称+等级+阶段）
- [x] 顶部导航栏显示：返回按钮 + 头像 + 昵称（大字号）+ 等级标签 + 门派 + 阶段标签
- [x] 阶段标签根据 `CharacterAttributesComponent.CurrentStage` 动态显示
- [x] 武侠能力显示青铜色，仙侠能力显示翡翠色，玄幻能力显示紫金色
- [x] 阶段标签悬停显示阶段描述 Tooltip（新增 HoverableInkTag 嵌套类，订阅 MouseEntered/MouseLeft 事件）

## 燕云十六声 + 魔兽世界风格融合
- [x] 整体布局采用魔兽世界骨架（左 3D 预览 + 装备槽环绕 + 右属性列表 + 底背包）
- [x] 视觉元素叠加燕云十六声风格（水墨晕染背景、金属蚀刻边框、五行色彩、繁体竖排标题）
- [x] 颜色主题与 `InkWashTheme` 保持一致，无色彩冲突
- [ ] 中文显示正常，无乱码（代码已实现，待运行时验证）

## 数据绑定
- [x] `BindCharacter(CharacterAttributesComponent)` 接口正常工作
- [x] 绑定后顶部信息、雷达图、属性数值从组件读取真实数据
- [x] 未绑定时回退到 mock 数据，UI 正常显示
- [x] 装备数据从 `EquipmentDatabase` 加载，mock 背包装备 8-12 件

## 编译与运行
- [x] Flax.Build 编译 0 错误（Game.CSharp.dll 成功生成 2.3MB，仅 XML 文档警告 CS0419/CS1574 等预存在项）
- [ ] Flax 编辑器中打开角色属性面板无异常（代码已实现，待运行时验证）
- [ ] 所有交互（拖拽旋转、双击换装、悬停 Tooltip）响应正常（代码已实现，待运行时验证）
- [ ] 无回归：现有 `MenuEquipmentPage`、`CombatHudPage` 等页面功能不受影响（`NavigationRequested` 事件已保留，待运行时验证）
